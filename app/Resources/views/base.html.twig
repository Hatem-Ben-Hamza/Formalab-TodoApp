<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Formalab|TodoApp{% endblock %}</title>
        {% block stylesheets %}
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
        <style>
        body {
        padding-top: 5rem;
        }
        </style>
        {% endblock %}
    </head>
    <body>
        
        <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
      <a class="navbar-brand" href="#">Formalab TodoApp</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarsExampleDefault">
        <ul class="navbar-nav mr-auto">
        
        <!-- Comme on a déja dis on a deux méthodes pour faire appel à une route -->
        
        <!-- On va utiliser au début la premiére méthode avec path et on deuxiéme lieux on va utiliser 
        l'autre méthode qui sera commentée -->

          <li class="nav-item {% if app.request.attributes.get('_route') == 'todo_homepage' %} active {% endif %}">
            <a class="nav-link" href="{{ path('todo_homepage') }}">Home</a>
          </li>

           <li class="nav-item {% if app.request.attributes.get('_route') == 'todo_create' %} active {% endif %}">
            <a class="nav-link" href="{{ path('todo_create') }}">Create Todo</a>
          </li>

           <li class="nav-item {% if app.request.attributes.get('_route') == 'todo_update' %} active {% endif %}">
            <a class="nav-link" href="{{ path('todo_update',{'id':7}) }}">Update Todo</a>
          </li>

          <!-- On peut utiliser là la méthode sans path 
          la méme déclaration la seule différence comme on a dis c'est au niveau de href 
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>

           <li class="nav-item">
            <a class="nav-link" href="/create">Create Todo</a>
          </li>

           <li class="nav-item">
            <a class="nav-link" href="/update/7">Update Todo</a>
          </li>
          -->

        </ul>
        
      </div>
    </nav>
      
        {% block body %}{% endblock %}
        {% block javascripts %}
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
        {% endblock %}
    </body>
</html>
